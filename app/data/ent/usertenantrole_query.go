// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"ncobase/app/data/ent/predicate"
	"ncobase/app/data/ent/usertenantrole"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserTenantRoleQuery is the builder for querying UserTenantRole entities.
type UserTenantRoleQuery struct {
	config
	ctx        *QueryContext
	order      []usertenantrole.OrderOption
	inters     []Interceptor
	predicates []predicate.UserTenantRole
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserTenantRoleQuery builder.
func (utrq *UserTenantRoleQuery) Where(ps ...predicate.UserTenantRole) *UserTenantRoleQuery {
	utrq.predicates = append(utrq.predicates, ps...)
	return utrq
}

// Limit the number of records to be returned by this query.
func (utrq *UserTenantRoleQuery) Limit(limit int) *UserTenantRoleQuery {
	utrq.ctx.Limit = &limit
	return utrq
}

// Offset to start from.
func (utrq *UserTenantRoleQuery) Offset(offset int) *UserTenantRoleQuery {
	utrq.ctx.Offset = &offset
	return utrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (utrq *UserTenantRoleQuery) Unique(unique bool) *UserTenantRoleQuery {
	utrq.ctx.Unique = &unique
	return utrq
}

// Order specifies how the records should be ordered.
func (utrq *UserTenantRoleQuery) Order(o ...usertenantrole.OrderOption) *UserTenantRoleQuery {
	utrq.order = append(utrq.order, o...)
	return utrq
}

// First returns the first UserTenantRole entity from the query.
// Returns a *NotFoundError when no UserTenantRole was found.
func (utrq *UserTenantRoleQuery) First(ctx context.Context) (*UserTenantRole, error) {
	nodes, err := utrq.Limit(1).All(setContextOp(ctx, utrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usertenantrole.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) FirstX(ctx context.Context) *UserTenantRole {
	node, err := utrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserTenantRole ID from the query.
// Returns a *NotFoundError when no UserTenantRole ID was found.
func (utrq *UserTenantRoleQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = utrq.Limit(1).IDs(setContextOp(ctx, utrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usertenantrole.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) FirstIDX(ctx context.Context) string {
	id, err := utrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserTenantRole entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserTenantRole entity is found.
// Returns a *NotFoundError when no UserTenantRole entities are found.
func (utrq *UserTenantRoleQuery) Only(ctx context.Context) (*UserTenantRole, error) {
	nodes, err := utrq.Limit(2).All(setContextOp(ctx, utrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usertenantrole.Label}
	default:
		return nil, &NotSingularError{usertenantrole.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) OnlyX(ctx context.Context) *UserTenantRole {
	node, err := utrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserTenantRole ID in the query.
// Returns a *NotSingularError when more than one UserTenantRole ID is found.
// Returns a *NotFoundError when no entities are found.
func (utrq *UserTenantRoleQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = utrq.Limit(2).IDs(setContextOp(ctx, utrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usertenantrole.Label}
	default:
		err = &NotSingularError{usertenantrole.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) OnlyIDX(ctx context.Context) string {
	id, err := utrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserTenantRoles.
func (utrq *UserTenantRoleQuery) All(ctx context.Context) ([]*UserTenantRole, error) {
	ctx = setContextOp(ctx, utrq.ctx, "All")
	if err := utrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserTenantRole, *UserTenantRoleQuery]()
	return withInterceptors[[]*UserTenantRole](ctx, utrq, qr, utrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) AllX(ctx context.Context) []*UserTenantRole {
	nodes, err := utrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserTenantRole IDs.
func (utrq *UserTenantRoleQuery) IDs(ctx context.Context) (ids []string, err error) {
	if utrq.ctx.Unique == nil && utrq.path != nil {
		utrq.Unique(true)
	}
	ctx = setContextOp(ctx, utrq.ctx, "IDs")
	if err = utrq.Select(usertenantrole.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) IDsX(ctx context.Context) []string {
	ids, err := utrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (utrq *UserTenantRoleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, utrq.ctx, "Count")
	if err := utrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, utrq, querierCount[*UserTenantRoleQuery](), utrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) CountX(ctx context.Context) int {
	count, err := utrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (utrq *UserTenantRoleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, utrq.ctx, "Exist")
	switch _, err := utrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (utrq *UserTenantRoleQuery) ExistX(ctx context.Context) bool {
	exist, err := utrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserTenantRoleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (utrq *UserTenantRoleQuery) Clone() *UserTenantRoleQuery {
	if utrq == nil {
		return nil
	}
	return &UserTenantRoleQuery{
		config:     utrq.config,
		ctx:        utrq.ctx.Clone(),
		order:      append([]usertenantrole.OrderOption{}, utrq.order...),
		inters:     append([]Interceptor{}, utrq.inters...),
		predicates: append([]predicate.UserTenantRole{}, utrq.predicates...),
		// clone intermediate query.
		sql:  utrq.sql.Clone(),
		path: utrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserTenantRole.Query().
//		GroupBy(usertenantrole.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (utrq *UserTenantRoleQuery) GroupBy(field string, fields ...string) *UserTenantRoleGroupBy {
	utrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserTenantRoleGroupBy{build: utrq}
	grbuild.flds = &utrq.ctx.Fields
	grbuild.label = usertenantrole.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//	}
//
//	client.UserTenantRole.Query().
//		Select(usertenantrole.FieldUserID).
//		Scan(ctx, &v)
func (utrq *UserTenantRoleQuery) Select(fields ...string) *UserTenantRoleSelect {
	utrq.ctx.Fields = append(utrq.ctx.Fields, fields...)
	sbuild := &UserTenantRoleSelect{UserTenantRoleQuery: utrq}
	sbuild.label = usertenantrole.Label
	sbuild.flds, sbuild.scan = &utrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserTenantRoleSelect configured with the given aggregations.
func (utrq *UserTenantRoleQuery) Aggregate(fns ...AggregateFunc) *UserTenantRoleSelect {
	return utrq.Select().Aggregate(fns...)
}

func (utrq *UserTenantRoleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range utrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, utrq); err != nil {
				return err
			}
		}
	}
	for _, f := range utrq.ctx.Fields {
		if !usertenantrole.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if utrq.path != nil {
		prev, err := utrq.path(ctx)
		if err != nil {
			return err
		}
		utrq.sql = prev
	}
	return nil
}

func (utrq *UserTenantRoleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserTenantRole, error) {
	var (
		nodes = []*UserTenantRole{}
		_spec = utrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserTenantRole).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserTenantRole{config: utrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, utrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (utrq *UserTenantRoleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := utrq.querySpec()
	_spec.Node.Columns = utrq.ctx.Fields
	if len(utrq.ctx.Fields) > 0 {
		_spec.Unique = utrq.ctx.Unique != nil && *utrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, utrq.driver, _spec)
}

func (utrq *UserTenantRoleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usertenantrole.Table, usertenantrole.Columns, sqlgraph.NewFieldSpec(usertenantrole.FieldID, field.TypeString))
	_spec.From = utrq.sql
	if unique := utrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if utrq.path != nil {
		_spec.Unique = true
	}
	if fields := utrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usertenantrole.FieldID)
		for i := range fields {
			if fields[i] != usertenantrole.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := utrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := utrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := utrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := utrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (utrq *UserTenantRoleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(utrq.driver.Dialect())
	t1 := builder.Table(usertenantrole.Table)
	columns := utrq.ctx.Fields
	if len(columns) == 0 {
		columns = usertenantrole.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if utrq.sql != nil {
		selector = utrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if utrq.ctx.Unique != nil && *utrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range utrq.predicates {
		p(selector)
	}
	for _, p := range utrq.order {
		p(selector)
	}
	if offset := utrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := utrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserTenantRoleGroupBy is the group-by builder for UserTenantRole entities.
type UserTenantRoleGroupBy struct {
	selector
	build *UserTenantRoleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (utrgb *UserTenantRoleGroupBy) Aggregate(fns ...AggregateFunc) *UserTenantRoleGroupBy {
	utrgb.fns = append(utrgb.fns, fns...)
	return utrgb
}

// Scan applies the selector query and scans the result into the given value.
func (utrgb *UserTenantRoleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utrgb.build.ctx, "GroupBy")
	if err := utrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTenantRoleQuery, *UserTenantRoleGroupBy](ctx, utrgb.build, utrgb, utrgb.build.inters, v)
}

func (utrgb *UserTenantRoleGroupBy) sqlScan(ctx context.Context, root *UserTenantRoleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(utrgb.fns))
	for _, fn := range utrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*utrgb.flds)+len(utrgb.fns))
		for _, f := range *utrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*utrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserTenantRoleSelect is the builder for selecting fields of UserTenantRole entities.
type UserTenantRoleSelect struct {
	*UserTenantRoleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (utrs *UserTenantRoleSelect) Aggregate(fns ...AggregateFunc) *UserTenantRoleSelect {
	utrs.fns = append(utrs.fns, fns...)
	return utrs
}

// Scan applies the selector query and scans the result into the given value.
func (utrs *UserTenantRoleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utrs.ctx, "Select")
	if err := utrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTenantRoleQuery, *UserTenantRoleSelect](ctx, utrs.UserTenantRoleQuery, utrs, utrs.inters, v)
}

func (utrs *UserTenantRoleSelect) sqlScan(ctx context.Context, root *UserTenantRoleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(utrs.fns))
	for _, fn := range utrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*utrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
