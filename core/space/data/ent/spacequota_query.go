// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"ncobase/space/data/ent/predicate"
	"ncobase/space/data/ent/spacequota"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SpaceQuotaQuery is the builder for querying SpaceQuota entities.
type SpaceQuotaQuery struct {
	config
	ctx        *QueryContext
	order      []spacequota.OrderOption
	inters     []Interceptor
	predicates []predicate.SpaceQuota
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SpaceQuotaQuery builder.
func (sqq *SpaceQuotaQuery) Where(ps ...predicate.SpaceQuota) *SpaceQuotaQuery {
	sqq.predicates = append(sqq.predicates, ps...)
	return sqq
}

// Limit the number of records to be returned by this query.
func (sqq *SpaceQuotaQuery) Limit(limit int) *SpaceQuotaQuery {
	sqq.ctx.Limit = &limit
	return sqq
}

// Offset to start from.
func (sqq *SpaceQuotaQuery) Offset(offset int) *SpaceQuotaQuery {
	sqq.ctx.Offset = &offset
	return sqq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sqq *SpaceQuotaQuery) Unique(unique bool) *SpaceQuotaQuery {
	sqq.ctx.Unique = &unique
	return sqq
}

// Order specifies how the records should be ordered.
func (sqq *SpaceQuotaQuery) Order(o ...spacequota.OrderOption) *SpaceQuotaQuery {
	sqq.order = append(sqq.order, o...)
	return sqq
}

// First returns the first SpaceQuota entity from the query.
// Returns a *NotFoundError when no SpaceQuota was found.
func (sqq *SpaceQuotaQuery) First(ctx context.Context) (*SpaceQuota, error) {
	nodes, err := sqq.Limit(1).All(setContextOp(ctx, sqq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{spacequota.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) FirstX(ctx context.Context) *SpaceQuota {
	node, err := sqq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SpaceQuota ID from the query.
// Returns a *NotFoundError when no SpaceQuota ID was found.
func (sqq *SpaceQuotaQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sqq.Limit(1).IDs(setContextOp(ctx, sqq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{spacequota.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) FirstIDX(ctx context.Context) string {
	id, err := sqq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SpaceQuota entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SpaceQuota entity is found.
// Returns a *NotFoundError when no SpaceQuota entities are found.
func (sqq *SpaceQuotaQuery) Only(ctx context.Context) (*SpaceQuota, error) {
	nodes, err := sqq.Limit(2).All(setContextOp(ctx, sqq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{spacequota.Label}
	default:
		return nil, &NotSingularError{spacequota.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) OnlyX(ctx context.Context) *SpaceQuota {
	node, err := sqq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SpaceQuota ID in the query.
// Returns a *NotSingularError when more than one SpaceQuota ID is found.
// Returns a *NotFoundError when no entities are found.
func (sqq *SpaceQuotaQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sqq.Limit(2).IDs(setContextOp(ctx, sqq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{spacequota.Label}
	default:
		err = &NotSingularError{spacequota.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) OnlyIDX(ctx context.Context) string {
	id, err := sqq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SpaceQuotaSlice.
func (sqq *SpaceQuotaQuery) All(ctx context.Context) ([]*SpaceQuota, error) {
	ctx = setContextOp(ctx, sqq.ctx, ent.OpQueryAll)
	if err := sqq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SpaceQuota, *SpaceQuotaQuery]()
	return withInterceptors[[]*SpaceQuota](ctx, sqq, qr, sqq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) AllX(ctx context.Context) []*SpaceQuota {
	nodes, err := sqq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SpaceQuota IDs.
func (sqq *SpaceQuotaQuery) IDs(ctx context.Context) (ids []string, err error) {
	if sqq.ctx.Unique == nil && sqq.path != nil {
		sqq.Unique(true)
	}
	ctx = setContextOp(ctx, sqq.ctx, ent.OpQueryIDs)
	if err = sqq.Select(spacequota.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) IDsX(ctx context.Context) []string {
	ids, err := sqq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sqq *SpaceQuotaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sqq.ctx, ent.OpQueryCount)
	if err := sqq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sqq, querierCount[*SpaceQuotaQuery](), sqq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) CountX(ctx context.Context) int {
	count, err := sqq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sqq *SpaceQuotaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sqq.ctx, ent.OpQueryExist)
	switch _, err := sqq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sqq *SpaceQuotaQuery) ExistX(ctx context.Context) bool {
	exist, err := sqq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SpaceQuotaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sqq *SpaceQuotaQuery) Clone() *SpaceQuotaQuery {
	if sqq == nil {
		return nil
	}
	return &SpaceQuotaQuery{
		config:     sqq.config,
		ctx:        sqq.ctx.Clone(),
		order:      append([]spacequota.OrderOption{}, sqq.order...),
		inters:     append([]Interceptor{}, sqq.inters...),
		predicates: append([]predicate.SpaceQuota{}, sqq.predicates...),
		// clone intermediate query.
		sql:  sqq.sql.Clone(),
		path: sqq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SpaceID string `json:"space_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SpaceQuota.Query().
//		GroupBy(spacequota.FieldSpaceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sqq *SpaceQuotaQuery) GroupBy(field string, fields ...string) *SpaceQuotaGroupBy {
	sqq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SpaceQuotaGroupBy{build: sqq}
	grbuild.flds = &sqq.ctx.Fields
	grbuild.label = spacequota.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SpaceID string `json:"space_id,omitempty"`
//	}
//
//	client.SpaceQuota.Query().
//		Select(spacequota.FieldSpaceID).
//		Scan(ctx, &v)
func (sqq *SpaceQuotaQuery) Select(fields ...string) *SpaceQuotaSelect {
	sqq.ctx.Fields = append(sqq.ctx.Fields, fields...)
	sbuild := &SpaceQuotaSelect{SpaceQuotaQuery: sqq}
	sbuild.label = spacequota.Label
	sbuild.flds, sbuild.scan = &sqq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SpaceQuotaSelect configured with the given aggregations.
func (sqq *SpaceQuotaQuery) Aggregate(fns ...AggregateFunc) *SpaceQuotaSelect {
	return sqq.Select().Aggregate(fns...)
}

func (sqq *SpaceQuotaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sqq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sqq); err != nil {
				return err
			}
		}
	}
	for _, f := range sqq.ctx.Fields {
		if !spacequota.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sqq.path != nil {
		prev, err := sqq.path(ctx)
		if err != nil {
			return err
		}
		sqq.sql = prev
	}
	return nil
}

func (sqq *SpaceQuotaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SpaceQuota, error) {
	var (
		nodes = []*SpaceQuota{}
		_spec = sqq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SpaceQuota).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SpaceQuota{config: sqq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sqq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sqq *SpaceQuotaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sqq.querySpec()
	_spec.Node.Columns = sqq.ctx.Fields
	if len(sqq.ctx.Fields) > 0 {
		_spec.Unique = sqq.ctx.Unique != nil && *sqq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sqq.driver, _spec)
}

func (sqq *SpaceQuotaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(spacequota.Table, spacequota.Columns, sqlgraph.NewFieldSpec(spacequota.FieldID, field.TypeString))
	_spec.From = sqq.sql
	if unique := sqq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sqq.path != nil {
		_spec.Unique = true
	}
	if fields := sqq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, spacequota.FieldID)
		for i := range fields {
			if fields[i] != spacequota.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sqq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sqq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sqq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sqq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sqq *SpaceQuotaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sqq.driver.Dialect())
	t1 := builder.Table(spacequota.Table)
	columns := sqq.ctx.Fields
	if len(columns) == 0 {
		columns = spacequota.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sqq.sql != nil {
		selector = sqq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sqq.ctx.Unique != nil && *sqq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sqq.predicates {
		p(selector)
	}
	for _, p := range sqq.order {
		p(selector)
	}
	if offset := sqq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sqq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SpaceQuotaGroupBy is the group-by builder for SpaceQuota entities.
type SpaceQuotaGroupBy struct {
	selector
	build *SpaceQuotaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sqgb *SpaceQuotaGroupBy) Aggregate(fns ...AggregateFunc) *SpaceQuotaGroupBy {
	sqgb.fns = append(sqgb.fns, fns...)
	return sqgb
}

// Scan applies the selector query and scans the result into the given value.
func (sqgb *SpaceQuotaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sqgb.build.ctx, ent.OpQueryGroupBy)
	if err := sqgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpaceQuotaQuery, *SpaceQuotaGroupBy](ctx, sqgb.build, sqgb, sqgb.build.inters, v)
}

func (sqgb *SpaceQuotaGroupBy) sqlScan(ctx context.Context, root *SpaceQuotaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sqgb.fns))
	for _, fn := range sqgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sqgb.flds)+len(sqgb.fns))
		for _, f := range *sqgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sqgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sqgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SpaceQuotaSelect is the builder for selecting fields of SpaceQuota entities.
type SpaceQuotaSelect struct {
	*SpaceQuotaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sqs *SpaceQuotaSelect) Aggregate(fns ...AggregateFunc) *SpaceQuotaSelect {
	sqs.fns = append(sqs.fns, fns...)
	return sqs
}

// Scan applies the selector query and scans the result into the given value.
func (sqs *SpaceQuotaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sqs.ctx, ent.OpQuerySelect)
	if err := sqs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SpaceQuotaQuery, *SpaceQuotaSelect](ctx, sqs.SpaceQuotaQuery, sqs, sqs.inters, v)
}

func (sqs *SpaceQuotaSelect) sqlScan(ctx context.Context, root *SpaceQuotaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sqs.fns))
	for _, fn := range sqs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sqs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sqs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
