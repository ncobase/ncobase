// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"ncobase/space/data/ent/predicate"
	"ncobase/space/data/ent/tenantgroup"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TenantGroupQuery is the builder for querying TenantGroup entities.
type TenantGroupQuery struct {
	config
	ctx        *QueryContext
	order      []tenantgroup.OrderOption
	inters     []Interceptor
	predicates []predicate.TenantGroup
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TenantGroupQuery builder.
func (tgq *TenantGroupQuery) Where(ps ...predicate.TenantGroup) *TenantGroupQuery {
	tgq.predicates = append(tgq.predicates, ps...)
	return tgq
}

// Limit the number of records to be returned by this query.
func (tgq *TenantGroupQuery) Limit(limit int) *TenantGroupQuery {
	tgq.ctx.Limit = &limit
	return tgq
}

// Offset to start from.
func (tgq *TenantGroupQuery) Offset(offset int) *TenantGroupQuery {
	tgq.ctx.Offset = &offset
	return tgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tgq *TenantGroupQuery) Unique(unique bool) *TenantGroupQuery {
	tgq.ctx.Unique = &unique
	return tgq
}

// Order specifies how the records should be ordered.
func (tgq *TenantGroupQuery) Order(o ...tenantgroup.OrderOption) *TenantGroupQuery {
	tgq.order = append(tgq.order, o...)
	return tgq
}

// First returns the first TenantGroup entity from the query.
// Returns a *NotFoundError when no TenantGroup was found.
func (tgq *TenantGroupQuery) First(ctx context.Context) (*TenantGroup, error) {
	nodes, err := tgq.Limit(1).All(setContextOp(ctx, tgq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tenantgroup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tgq *TenantGroupQuery) FirstX(ctx context.Context) *TenantGroup {
	node, err := tgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TenantGroup ID from the query.
// Returns a *NotFoundError when no TenantGroup ID was found.
func (tgq *TenantGroupQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = tgq.Limit(1).IDs(setContextOp(ctx, tgq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tenantgroup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tgq *TenantGroupQuery) FirstIDX(ctx context.Context) string {
	id, err := tgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TenantGroup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TenantGroup entity is found.
// Returns a *NotFoundError when no TenantGroup entities are found.
func (tgq *TenantGroupQuery) Only(ctx context.Context) (*TenantGroup, error) {
	nodes, err := tgq.Limit(2).All(setContextOp(ctx, tgq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tenantgroup.Label}
	default:
		return nil, &NotSingularError{tenantgroup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tgq *TenantGroupQuery) OnlyX(ctx context.Context) *TenantGroup {
	node, err := tgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TenantGroup ID in the query.
// Returns a *NotSingularError when more than one TenantGroup ID is found.
// Returns a *NotFoundError when no entities are found.
func (tgq *TenantGroupQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = tgq.Limit(2).IDs(setContextOp(ctx, tgq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tenantgroup.Label}
	default:
		err = &NotSingularError{tenantgroup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tgq *TenantGroupQuery) OnlyIDX(ctx context.Context) string {
	id, err := tgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TenantGroups.
func (tgq *TenantGroupQuery) All(ctx context.Context) ([]*TenantGroup, error) {
	ctx = setContextOp(ctx, tgq.ctx, ent.OpQueryAll)
	if err := tgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TenantGroup, *TenantGroupQuery]()
	return withInterceptors[[]*TenantGroup](ctx, tgq, qr, tgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tgq *TenantGroupQuery) AllX(ctx context.Context) []*TenantGroup {
	nodes, err := tgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TenantGroup IDs.
func (tgq *TenantGroupQuery) IDs(ctx context.Context) (ids []string, err error) {
	if tgq.ctx.Unique == nil && tgq.path != nil {
		tgq.Unique(true)
	}
	ctx = setContextOp(ctx, tgq.ctx, ent.OpQueryIDs)
	if err = tgq.Select(tenantgroup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tgq *TenantGroupQuery) IDsX(ctx context.Context) []string {
	ids, err := tgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tgq *TenantGroupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tgq.ctx, ent.OpQueryCount)
	if err := tgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tgq, querierCount[*TenantGroupQuery](), tgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tgq *TenantGroupQuery) CountX(ctx context.Context) int {
	count, err := tgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tgq *TenantGroupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tgq.ctx, ent.OpQueryExist)
	switch _, err := tgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tgq *TenantGroupQuery) ExistX(ctx context.Context) bool {
	exist, err := tgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TenantGroupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tgq *TenantGroupQuery) Clone() *TenantGroupQuery {
	if tgq == nil {
		return nil
	}
	return &TenantGroupQuery{
		config:     tgq.config,
		ctx:        tgq.ctx.Clone(),
		order:      append([]tenantgroup.OrderOption{}, tgq.order...),
		inters:     append([]Interceptor{}, tgq.inters...),
		predicates: append([]predicate.TenantGroup{}, tgq.predicates...),
		// clone intermediate query.
		sql:  tgq.sql.Clone(),
		path: tgq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TenantGroup.Query().
//		GroupBy(tenantgroup.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tgq *TenantGroupQuery) GroupBy(field string, fields ...string) *TenantGroupGroupBy {
	tgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TenantGroupGroupBy{build: tgq}
	grbuild.flds = &tgq.ctx.Fields
	grbuild.label = tenantgroup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.TenantGroup.Query().
//		Select(tenantgroup.FieldTenantID).
//		Scan(ctx, &v)
func (tgq *TenantGroupQuery) Select(fields ...string) *TenantGroupSelect {
	tgq.ctx.Fields = append(tgq.ctx.Fields, fields...)
	sbuild := &TenantGroupSelect{TenantGroupQuery: tgq}
	sbuild.label = tenantgroup.Label
	sbuild.flds, sbuild.scan = &tgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TenantGroupSelect configured with the given aggregations.
func (tgq *TenantGroupQuery) Aggregate(fns ...AggregateFunc) *TenantGroupSelect {
	return tgq.Select().Aggregate(fns...)
}

func (tgq *TenantGroupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tgq); err != nil {
				return err
			}
		}
	}
	for _, f := range tgq.ctx.Fields {
		if !tenantgroup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tgq.path != nil {
		prev, err := tgq.path(ctx)
		if err != nil {
			return err
		}
		tgq.sql = prev
	}
	return nil
}

func (tgq *TenantGroupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TenantGroup, error) {
	var (
		nodes = []*TenantGroup{}
		_spec = tgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TenantGroup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TenantGroup{config: tgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tgq *TenantGroupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tgq.querySpec()
	_spec.Node.Columns = tgq.ctx.Fields
	if len(tgq.ctx.Fields) > 0 {
		_spec.Unique = tgq.ctx.Unique != nil && *tgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tgq.driver, _spec)
}

func (tgq *TenantGroupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tenantgroup.Table, tenantgroup.Columns, sqlgraph.NewFieldSpec(tenantgroup.FieldID, field.TypeString))
	_spec.From = tgq.sql
	if unique := tgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tgq.path != nil {
		_spec.Unique = true
	}
	if fields := tgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tenantgroup.FieldID)
		for i := range fields {
			if fields[i] != tenantgroup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tgq *TenantGroupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tgq.driver.Dialect())
	t1 := builder.Table(tenantgroup.Table)
	columns := tgq.ctx.Fields
	if len(columns) == 0 {
		columns = tenantgroup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tgq.sql != nil {
		selector = tgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tgq.ctx.Unique != nil && *tgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tgq.predicates {
		p(selector)
	}
	for _, p := range tgq.order {
		p(selector)
	}
	if offset := tgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TenantGroupGroupBy is the group-by builder for TenantGroup entities.
type TenantGroupGroupBy struct {
	selector
	build *TenantGroupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tggb *TenantGroupGroupBy) Aggregate(fns ...AggregateFunc) *TenantGroupGroupBy {
	tggb.fns = append(tggb.fns, fns...)
	return tggb
}

// Scan applies the selector query and scans the result into the given value.
func (tggb *TenantGroupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tggb.build.ctx, ent.OpQueryGroupBy)
	if err := tggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TenantGroupQuery, *TenantGroupGroupBy](ctx, tggb.build, tggb, tggb.build.inters, v)
}

func (tggb *TenantGroupGroupBy) sqlScan(ctx context.Context, root *TenantGroupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tggb.fns))
	for _, fn := range tggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tggb.flds)+len(tggb.fns))
		for _, f := range *tggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TenantGroupSelect is the builder for selecting fields of TenantGroup entities.
type TenantGroupSelect struct {
	*TenantGroupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tgs *TenantGroupSelect) Aggregate(fns ...AggregateFunc) *TenantGroupSelect {
	tgs.fns = append(tgs.fns, fns...)
	return tgs
}

// Scan applies the selector query and scans the result into the given value.
func (tgs *TenantGroupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tgs.ctx, ent.OpQuerySelect)
	if err := tgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TenantGroupQuery, *TenantGroupSelect](ctx, tgs.TenantGroupQuery, tgs, tgs.inters, v)
}

func (tgs *TenantGroupSelect) sqlScan(ctx context.Context, root *TenantGroupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tgs.fns))
	for _, fn := range tgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
