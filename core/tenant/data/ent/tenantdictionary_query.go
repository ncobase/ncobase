// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"ncobase/tenant/data/ent/predicate"
	"ncobase/tenant/data/ent/tenantdictionary"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TenantDictionaryQuery is the builder for querying TenantDictionary entities.
type TenantDictionaryQuery struct {
	config
	ctx        *QueryContext
	order      []tenantdictionary.OrderOption
	inters     []Interceptor
	predicates []predicate.TenantDictionary
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TenantDictionaryQuery builder.
func (tdq *TenantDictionaryQuery) Where(ps ...predicate.TenantDictionary) *TenantDictionaryQuery {
	tdq.predicates = append(tdq.predicates, ps...)
	return tdq
}

// Limit the number of records to be returned by this query.
func (tdq *TenantDictionaryQuery) Limit(limit int) *TenantDictionaryQuery {
	tdq.ctx.Limit = &limit
	return tdq
}

// Offset to start from.
func (tdq *TenantDictionaryQuery) Offset(offset int) *TenantDictionaryQuery {
	tdq.ctx.Offset = &offset
	return tdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tdq *TenantDictionaryQuery) Unique(unique bool) *TenantDictionaryQuery {
	tdq.ctx.Unique = &unique
	return tdq
}

// Order specifies how the records should be ordered.
func (tdq *TenantDictionaryQuery) Order(o ...tenantdictionary.OrderOption) *TenantDictionaryQuery {
	tdq.order = append(tdq.order, o...)
	return tdq
}

// First returns the first TenantDictionary entity from the query.
// Returns a *NotFoundError when no TenantDictionary was found.
func (tdq *TenantDictionaryQuery) First(ctx context.Context) (*TenantDictionary, error) {
	nodes, err := tdq.Limit(1).All(setContextOp(ctx, tdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tenantdictionary.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) FirstX(ctx context.Context) *TenantDictionary {
	node, err := tdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TenantDictionary ID from the query.
// Returns a *NotFoundError when no TenantDictionary ID was found.
func (tdq *TenantDictionaryQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = tdq.Limit(1).IDs(setContextOp(ctx, tdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tenantdictionary.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) FirstIDX(ctx context.Context) string {
	id, err := tdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TenantDictionary entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TenantDictionary entity is found.
// Returns a *NotFoundError when no TenantDictionary entities are found.
func (tdq *TenantDictionaryQuery) Only(ctx context.Context) (*TenantDictionary, error) {
	nodes, err := tdq.Limit(2).All(setContextOp(ctx, tdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tenantdictionary.Label}
	default:
		return nil, &NotSingularError{tenantdictionary.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) OnlyX(ctx context.Context) *TenantDictionary {
	node, err := tdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TenantDictionary ID in the query.
// Returns a *NotSingularError when more than one TenantDictionary ID is found.
// Returns a *NotFoundError when no entities are found.
func (tdq *TenantDictionaryQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = tdq.Limit(2).IDs(setContextOp(ctx, tdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tenantdictionary.Label}
	default:
		err = &NotSingularError{tenantdictionary.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) OnlyIDX(ctx context.Context) string {
	id, err := tdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TenantDictionaries.
func (tdq *TenantDictionaryQuery) All(ctx context.Context) ([]*TenantDictionary, error) {
	ctx = setContextOp(ctx, tdq.ctx, ent.OpQueryAll)
	if err := tdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TenantDictionary, *TenantDictionaryQuery]()
	return withInterceptors[[]*TenantDictionary](ctx, tdq, qr, tdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) AllX(ctx context.Context) []*TenantDictionary {
	nodes, err := tdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TenantDictionary IDs.
func (tdq *TenantDictionaryQuery) IDs(ctx context.Context) (ids []string, err error) {
	if tdq.ctx.Unique == nil && tdq.path != nil {
		tdq.Unique(true)
	}
	ctx = setContextOp(ctx, tdq.ctx, ent.OpQueryIDs)
	if err = tdq.Select(tenantdictionary.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) IDsX(ctx context.Context) []string {
	ids, err := tdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tdq *TenantDictionaryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tdq.ctx, ent.OpQueryCount)
	if err := tdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tdq, querierCount[*TenantDictionaryQuery](), tdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) CountX(ctx context.Context) int {
	count, err := tdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tdq *TenantDictionaryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tdq.ctx, ent.OpQueryExist)
	switch _, err := tdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tdq *TenantDictionaryQuery) ExistX(ctx context.Context) bool {
	exist, err := tdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TenantDictionaryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tdq *TenantDictionaryQuery) Clone() *TenantDictionaryQuery {
	if tdq == nil {
		return nil
	}
	return &TenantDictionaryQuery{
		config:     tdq.config,
		ctx:        tdq.ctx.Clone(),
		order:      append([]tenantdictionary.OrderOption{}, tdq.order...),
		inters:     append([]Interceptor{}, tdq.inters...),
		predicates: append([]predicate.TenantDictionary{}, tdq.predicates...),
		// clone intermediate query.
		sql:  tdq.sql.Clone(),
		path: tdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TenantDictionary.Query().
//		GroupBy(tenantdictionary.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tdq *TenantDictionaryQuery) GroupBy(field string, fields ...string) *TenantDictionaryGroupBy {
	tdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TenantDictionaryGroupBy{build: tdq}
	grbuild.flds = &tdq.ctx.Fields
	grbuild.label = tenantdictionary.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.TenantDictionary.Query().
//		Select(tenantdictionary.FieldTenantID).
//		Scan(ctx, &v)
func (tdq *TenantDictionaryQuery) Select(fields ...string) *TenantDictionarySelect {
	tdq.ctx.Fields = append(tdq.ctx.Fields, fields...)
	sbuild := &TenantDictionarySelect{TenantDictionaryQuery: tdq}
	sbuild.label = tenantdictionary.Label
	sbuild.flds, sbuild.scan = &tdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TenantDictionarySelect configured with the given aggregations.
func (tdq *TenantDictionaryQuery) Aggregate(fns ...AggregateFunc) *TenantDictionarySelect {
	return tdq.Select().Aggregate(fns...)
}

func (tdq *TenantDictionaryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tdq); err != nil {
				return err
			}
		}
	}
	for _, f := range tdq.ctx.Fields {
		if !tenantdictionary.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tdq.path != nil {
		prev, err := tdq.path(ctx)
		if err != nil {
			return err
		}
		tdq.sql = prev
	}
	return nil
}

func (tdq *TenantDictionaryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TenantDictionary, error) {
	var (
		nodes = []*TenantDictionary{}
		_spec = tdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TenantDictionary).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TenantDictionary{config: tdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tdq *TenantDictionaryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tdq.querySpec()
	_spec.Node.Columns = tdq.ctx.Fields
	if len(tdq.ctx.Fields) > 0 {
		_spec.Unique = tdq.ctx.Unique != nil && *tdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tdq.driver, _spec)
}

func (tdq *TenantDictionaryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tenantdictionary.Table, tenantdictionary.Columns, sqlgraph.NewFieldSpec(tenantdictionary.FieldID, field.TypeString))
	_spec.From = tdq.sql
	if unique := tdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tdq.path != nil {
		_spec.Unique = true
	}
	if fields := tdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tenantdictionary.FieldID)
		for i := range fields {
			if fields[i] != tenantdictionary.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tdq *TenantDictionaryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tdq.driver.Dialect())
	t1 := builder.Table(tenantdictionary.Table)
	columns := tdq.ctx.Fields
	if len(columns) == 0 {
		columns = tenantdictionary.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tdq.sql != nil {
		selector = tdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tdq.ctx.Unique != nil && *tdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tdq.predicates {
		p(selector)
	}
	for _, p := range tdq.order {
		p(selector)
	}
	if offset := tdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TenantDictionaryGroupBy is the group-by builder for TenantDictionary entities.
type TenantDictionaryGroupBy struct {
	selector
	build *TenantDictionaryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tdgb *TenantDictionaryGroupBy) Aggregate(fns ...AggregateFunc) *TenantDictionaryGroupBy {
	tdgb.fns = append(tdgb.fns, fns...)
	return tdgb
}

// Scan applies the selector query and scans the result into the given value.
func (tdgb *TenantDictionaryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tdgb.build.ctx, ent.OpQueryGroupBy)
	if err := tdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TenantDictionaryQuery, *TenantDictionaryGroupBy](ctx, tdgb.build, tdgb, tdgb.build.inters, v)
}

func (tdgb *TenantDictionaryGroupBy) sqlScan(ctx context.Context, root *TenantDictionaryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tdgb.fns))
	for _, fn := range tdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tdgb.flds)+len(tdgb.fns))
		for _, f := range *tdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TenantDictionarySelect is the builder for selecting fields of TenantDictionary entities.
type TenantDictionarySelect struct {
	*TenantDictionaryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tds *TenantDictionarySelect) Aggregate(fns ...AggregateFunc) *TenantDictionarySelect {
	tds.fns = append(tds.fns, fns...)
	return tds
}

// Scan applies the selector query and scans the result into the given value.
func (tds *TenantDictionarySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tds.ctx, ent.OpQuerySelect)
	if err := tds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TenantDictionaryQuery, *TenantDictionarySelect](ctx, tds.TenantDictionaryQuery, tds, tds.inters, v)
}

func (tds *TenantDictionarySelect) sqlScan(ctx context.Context, root *TenantDictionaryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tds.fns))
	for _, fn := range tds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
