// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"ncobase/tenant/data/ent/predicate"
	"ncobase/tenant/data/ent/tenantquota"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TenantQuotaQuery is the builder for querying TenantQuota entities.
type TenantQuotaQuery struct {
	config
	ctx        *QueryContext
	order      []tenantquota.OrderOption
	inters     []Interceptor
	predicates []predicate.TenantQuota
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TenantQuotaQuery builder.
func (tqq *TenantQuotaQuery) Where(ps ...predicate.TenantQuota) *TenantQuotaQuery {
	tqq.predicates = append(tqq.predicates, ps...)
	return tqq
}

// Limit the number of records to be returned by this query.
func (tqq *TenantQuotaQuery) Limit(limit int) *TenantQuotaQuery {
	tqq.ctx.Limit = &limit
	return tqq
}

// Offset to start from.
func (tqq *TenantQuotaQuery) Offset(offset int) *TenantQuotaQuery {
	tqq.ctx.Offset = &offset
	return tqq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tqq *TenantQuotaQuery) Unique(unique bool) *TenantQuotaQuery {
	tqq.ctx.Unique = &unique
	return tqq
}

// Order specifies how the records should be ordered.
func (tqq *TenantQuotaQuery) Order(o ...tenantquota.OrderOption) *TenantQuotaQuery {
	tqq.order = append(tqq.order, o...)
	return tqq
}

// First returns the first TenantQuota entity from the query.
// Returns a *NotFoundError when no TenantQuota was found.
func (tqq *TenantQuotaQuery) First(ctx context.Context) (*TenantQuota, error) {
	nodes, err := tqq.Limit(1).All(setContextOp(ctx, tqq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tenantquota.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tqq *TenantQuotaQuery) FirstX(ctx context.Context) *TenantQuota {
	node, err := tqq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TenantQuota ID from the query.
// Returns a *NotFoundError when no TenantQuota ID was found.
func (tqq *TenantQuotaQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = tqq.Limit(1).IDs(setContextOp(ctx, tqq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tenantquota.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tqq *TenantQuotaQuery) FirstIDX(ctx context.Context) string {
	id, err := tqq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TenantQuota entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TenantQuota entity is found.
// Returns a *NotFoundError when no TenantQuota entities are found.
func (tqq *TenantQuotaQuery) Only(ctx context.Context) (*TenantQuota, error) {
	nodes, err := tqq.Limit(2).All(setContextOp(ctx, tqq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tenantquota.Label}
	default:
		return nil, &NotSingularError{tenantquota.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tqq *TenantQuotaQuery) OnlyX(ctx context.Context) *TenantQuota {
	node, err := tqq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TenantQuota ID in the query.
// Returns a *NotSingularError when more than one TenantQuota ID is found.
// Returns a *NotFoundError when no entities are found.
func (tqq *TenantQuotaQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = tqq.Limit(2).IDs(setContextOp(ctx, tqq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tenantquota.Label}
	default:
		err = &NotSingularError{tenantquota.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tqq *TenantQuotaQuery) OnlyIDX(ctx context.Context) string {
	id, err := tqq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TenantQuotaSlice.
func (tqq *TenantQuotaQuery) All(ctx context.Context) ([]*TenantQuota, error) {
	ctx = setContextOp(ctx, tqq.ctx, ent.OpQueryAll)
	if err := tqq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TenantQuota, *TenantQuotaQuery]()
	return withInterceptors[[]*TenantQuota](ctx, tqq, qr, tqq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tqq *TenantQuotaQuery) AllX(ctx context.Context) []*TenantQuota {
	nodes, err := tqq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TenantQuota IDs.
func (tqq *TenantQuotaQuery) IDs(ctx context.Context) (ids []string, err error) {
	if tqq.ctx.Unique == nil && tqq.path != nil {
		tqq.Unique(true)
	}
	ctx = setContextOp(ctx, tqq.ctx, ent.OpQueryIDs)
	if err = tqq.Select(tenantquota.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tqq *TenantQuotaQuery) IDsX(ctx context.Context) []string {
	ids, err := tqq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tqq *TenantQuotaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tqq.ctx, ent.OpQueryCount)
	if err := tqq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tqq, querierCount[*TenantQuotaQuery](), tqq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tqq *TenantQuotaQuery) CountX(ctx context.Context) int {
	count, err := tqq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tqq *TenantQuotaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tqq.ctx, ent.OpQueryExist)
	switch _, err := tqq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tqq *TenantQuotaQuery) ExistX(ctx context.Context) bool {
	exist, err := tqq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TenantQuotaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tqq *TenantQuotaQuery) Clone() *TenantQuotaQuery {
	if tqq == nil {
		return nil
	}
	return &TenantQuotaQuery{
		config:     tqq.config,
		ctx:        tqq.ctx.Clone(),
		order:      append([]tenantquota.OrderOption{}, tqq.order...),
		inters:     append([]Interceptor{}, tqq.inters...),
		predicates: append([]predicate.TenantQuota{}, tqq.predicates...),
		// clone intermediate query.
		sql:  tqq.sql.Clone(),
		path: tqq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TenantQuota.Query().
//		GroupBy(tenantquota.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tqq *TenantQuotaQuery) GroupBy(field string, fields ...string) *TenantQuotaGroupBy {
	tqq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TenantQuotaGroupBy{build: tqq}
	grbuild.flds = &tqq.ctx.Fields
	grbuild.label = tenantquota.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.TenantQuota.Query().
//		Select(tenantquota.FieldTenantID).
//		Scan(ctx, &v)
func (tqq *TenantQuotaQuery) Select(fields ...string) *TenantQuotaSelect {
	tqq.ctx.Fields = append(tqq.ctx.Fields, fields...)
	sbuild := &TenantQuotaSelect{TenantQuotaQuery: tqq}
	sbuild.label = tenantquota.Label
	sbuild.flds, sbuild.scan = &tqq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TenantQuotaSelect configured with the given aggregations.
func (tqq *TenantQuotaQuery) Aggregate(fns ...AggregateFunc) *TenantQuotaSelect {
	return tqq.Select().Aggregate(fns...)
}

func (tqq *TenantQuotaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tqq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tqq); err != nil {
				return err
			}
		}
	}
	for _, f := range tqq.ctx.Fields {
		if !tenantquota.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tqq.path != nil {
		prev, err := tqq.path(ctx)
		if err != nil {
			return err
		}
		tqq.sql = prev
	}
	return nil
}

func (tqq *TenantQuotaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TenantQuota, error) {
	var (
		nodes = []*TenantQuota{}
		_spec = tqq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TenantQuota).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TenantQuota{config: tqq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tqq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tqq *TenantQuotaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tqq.querySpec()
	_spec.Node.Columns = tqq.ctx.Fields
	if len(tqq.ctx.Fields) > 0 {
		_spec.Unique = tqq.ctx.Unique != nil && *tqq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tqq.driver, _spec)
}

func (tqq *TenantQuotaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tenantquota.Table, tenantquota.Columns, sqlgraph.NewFieldSpec(tenantquota.FieldID, field.TypeString))
	_spec.From = tqq.sql
	if unique := tqq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tqq.path != nil {
		_spec.Unique = true
	}
	if fields := tqq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tenantquota.FieldID)
		for i := range fields {
			if fields[i] != tenantquota.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tqq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tqq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tqq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tqq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tqq *TenantQuotaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tqq.driver.Dialect())
	t1 := builder.Table(tenantquota.Table)
	columns := tqq.ctx.Fields
	if len(columns) == 0 {
		columns = tenantquota.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tqq.sql != nil {
		selector = tqq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tqq.ctx.Unique != nil && *tqq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tqq.predicates {
		p(selector)
	}
	for _, p := range tqq.order {
		p(selector)
	}
	if offset := tqq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tqq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TenantQuotaGroupBy is the group-by builder for TenantQuota entities.
type TenantQuotaGroupBy struct {
	selector
	build *TenantQuotaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tqgb *TenantQuotaGroupBy) Aggregate(fns ...AggregateFunc) *TenantQuotaGroupBy {
	tqgb.fns = append(tqgb.fns, fns...)
	return tqgb
}

// Scan applies the selector query and scans the result into the given value.
func (tqgb *TenantQuotaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tqgb.build.ctx, ent.OpQueryGroupBy)
	if err := tqgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TenantQuotaQuery, *TenantQuotaGroupBy](ctx, tqgb.build, tqgb, tqgb.build.inters, v)
}

func (tqgb *TenantQuotaGroupBy) sqlScan(ctx context.Context, root *TenantQuotaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tqgb.fns))
	for _, fn := range tqgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tqgb.flds)+len(tqgb.fns))
		for _, f := range *tqgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tqgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tqgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TenantQuotaSelect is the builder for selecting fields of TenantQuota entities.
type TenantQuotaSelect struct {
	*TenantQuotaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tqs *TenantQuotaSelect) Aggregate(fns ...AggregateFunc) *TenantQuotaSelect {
	tqs.fns = append(tqs.fns, fns...)
	return tqs
}

// Scan applies the selector query and scans the result into the given value.
func (tqs *TenantQuotaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tqs.ctx, ent.OpQuerySelect)
	if err := tqs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TenantQuotaQuery, *TenantQuotaSelect](ctx, tqs.TenantQuotaQuery, tqs, tqs.inters, v)
}

func (tqs *TenantQuotaSelect) sqlScan(ctx context.Context, root *TenantQuotaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tqs.fns))
	for _, fn := range tqs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tqs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tqs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
