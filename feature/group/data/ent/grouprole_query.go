// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"ncobase/feature/group/data/ent/grouprole"
	"ncobase/feature/group/data/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GroupRoleQuery is the builder for querying GroupRole entities.
type GroupRoleQuery struct {
	config
	ctx        *QueryContext
	order      []grouprole.OrderOption
	inters     []Interceptor
	predicates []predicate.GroupRole
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GroupRoleQuery builder.
func (grq *GroupRoleQuery) Where(ps ...predicate.GroupRole) *GroupRoleQuery {
	grq.predicates = append(grq.predicates, ps...)
	return grq
}

// Limit the number of records to be returned by this query.
func (grq *GroupRoleQuery) Limit(limit int) *GroupRoleQuery {
	grq.ctx.Limit = &limit
	return grq
}

// Offset to start from.
func (grq *GroupRoleQuery) Offset(offset int) *GroupRoleQuery {
	grq.ctx.Offset = &offset
	return grq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (grq *GroupRoleQuery) Unique(unique bool) *GroupRoleQuery {
	grq.ctx.Unique = &unique
	return grq
}

// Order specifies how the records should be ordered.
func (grq *GroupRoleQuery) Order(o ...grouprole.OrderOption) *GroupRoleQuery {
	grq.order = append(grq.order, o...)
	return grq
}

// First returns the first GroupRole entity from the query.
// Returns a *NotFoundError when no GroupRole was found.
func (grq *GroupRoleQuery) First(ctx context.Context) (*GroupRole, error) {
	nodes, err := grq.Limit(1).All(setContextOp(ctx, grq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{grouprole.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (grq *GroupRoleQuery) FirstX(ctx context.Context) *GroupRole {
	node, err := grq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GroupRole ID from the query.
// Returns a *NotFoundError when no GroupRole ID was found.
func (grq *GroupRoleQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = grq.Limit(1).IDs(setContextOp(ctx, grq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{grouprole.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (grq *GroupRoleQuery) FirstIDX(ctx context.Context) string {
	id, err := grq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GroupRole entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GroupRole entity is found.
// Returns a *NotFoundError when no GroupRole entities are found.
func (grq *GroupRoleQuery) Only(ctx context.Context) (*GroupRole, error) {
	nodes, err := grq.Limit(2).All(setContextOp(ctx, grq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{grouprole.Label}
	default:
		return nil, &NotSingularError{grouprole.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (grq *GroupRoleQuery) OnlyX(ctx context.Context) *GroupRole {
	node, err := grq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GroupRole ID in the query.
// Returns a *NotSingularError when more than one GroupRole ID is found.
// Returns a *NotFoundError when no entities are found.
func (grq *GroupRoleQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = grq.Limit(2).IDs(setContextOp(ctx, grq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{grouprole.Label}
	default:
		err = &NotSingularError{grouprole.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (grq *GroupRoleQuery) OnlyIDX(ctx context.Context) string {
	id, err := grq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GroupRoles.
func (grq *GroupRoleQuery) All(ctx context.Context) ([]*GroupRole, error) {
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryAll)
	if err := grq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GroupRole, *GroupRoleQuery]()
	return withInterceptors[[]*GroupRole](ctx, grq, qr, grq.inters)
}

// AllX is like All, but panics if an error occurs.
func (grq *GroupRoleQuery) AllX(ctx context.Context) []*GroupRole {
	nodes, err := grq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GroupRole IDs.
func (grq *GroupRoleQuery) IDs(ctx context.Context) (ids []string, err error) {
	if grq.ctx.Unique == nil && grq.path != nil {
		grq.Unique(true)
	}
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryIDs)
	if err = grq.Select(grouprole.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (grq *GroupRoleQuery) IDsX(ctx context.Context) []string {
	ids, err := grq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (grq *GroupRoleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryCount)
	if err := grq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, grq, querierCount[*GroupRoleQuery](), grq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (grq *GroupRoleQuery) CountX(ctx context.Context) int {
	count, err := grq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (grq *GroupRoleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, grq.ctx, ent.OpQueryExist)
	switch _, err := grq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (grq *GroupRoleQuery) ExistX(ctx context.Context) bool {
	exist, err := grq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GroupRoleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (grq *GroupRoleQuery) Clone() *GroupRoleQuery {
	if grq == nil {
		return nil
	}
	return &GroupRoleQuery{
		config:     grq.config,
		ctx:        grq.ctx.Clone(),
		order:      append([]grouprole.OrderOption{}, grq.order...),
		inters:     append([]Interceptor{}, grq.inters...),
		predicates: append([]predicate.GroupRole{}, grq.predicates...),
		// clone intermediate query.
		sql:  grq.sql.Clone(),
		path: grq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GroupID string `json:"group_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GroupRole.Query().
//		GroupBy(grouprole.FieldGroupID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (grq *GroupRoleQuery) GroupBy(field string, fields ...string) *GroupRoleGroupBy {
	grq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GroupRoleGroupBy{build: grq}
	grbuild.flds = &grq.ctx.Fields
	grbuild.label = grouprole.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GroupID string `json:"group_id,omitempty"`
//	}
//
//	client.GroupRole.Query().
//		Select(grouprole.FieldGroupID).
//		Scan(ctx, &v)
func (grq *GroupRoleQuery) Select(fields ...string) *GroupRoleSelect {
	grq.ctx.Fields = append(grq.ctx.Fields, fields...)
	sbuild := &GroupRoleSelect{GroupRoleQuery: grq}
	sbuild.label = grouprole.Label
	sbuild.flds, sbuild.scan = &grq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GroupRoleSelect configured with the given aggregations.
func (grq *GroupRoleQuery) Aggregate(fns ...AggregateFunc) *GroupRoleSelect {
	return grq.Select().Aggregate(fns...)
}

func (grq *GroupRoleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range grq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, grq); err != nil {
				return err
			}
		}
	}
	for _, f := range grq.ctx.Fields {
		if !grouprole.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if grq.path != nil {
		prev, err := grq.path(ctx)
		if err != nil {
			return err
		}
		grq.sql = prev
	}
	return nil
}

func (grq *GroupRoleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GroupRole, error) {
	var (
		nodes = []*GroupRole{}
		_spec = grq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GroupRole).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GroupRole{config: grq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, grq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (grq *GroupRoleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := grq.querySpec()
	_spec.Node.Columns = grq.ctx.Fields
	if len(grq.ctx.Fields) > 0 {
		_spec.Unique = grq.ctx.Unique != nil && *grq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, grq.driver, _spec)
}

func (grq *GroupRoleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(grouprole.Table, grouprole.Columns, sqlgraph.NewFieldSpec(grouprole.FieldID, field.TypeString))
	_spec.From = grq.sql
	if unique := grq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if grq.path != nil {
		_spec.Unique = true
	}
	if fields := grq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, grouprole.FieldID)
		for i := range fields {
			if fields[i] != grouprole.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := grq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := grq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := grq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := grq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (grq *GroupRoleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(grq.driver.Dialect())
	t1 := builder.Table(grouprole.Table)
	columns := grq.ctx.Fields
	if len(columns) == 0 {
		columns = grouprole.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if grq.sql != nil {
		selector = grq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if grq.ctx.Unique != nil && *grq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range grq.predicates {
		p(selector)
	}
	for _, p := range grq.order {
		p(selector)
	}
	if offset := grq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := grq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GroupRoleGroupBy is the group-by builder for GroupRole entities.
type GroupRoleGroupBy struct {
	selector
	build *GroupRoleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (grgb *GroupRoleGroupBy) Aggregate(fns ...AggregateFunc) *GroupRoleGroupBy {
	grgb.fns = append(grgb.fns, fns...)
	return grgb
}

// Scan applies the selector query and scans the result into the given value.
func (grgb *GroupRoleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, grgb.build.ctx, ent.OpQueryGroupBy)
	if err := grgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GroupRoleQuery, *GroupRoleGroupBy](ctx, grgb.build, grgb, grgb.build.inters, v)
}

func (grgb *GroupRoleGroupBy) sqlScan(ctx context.Context, root *GroupRoleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(grgb.fns))
	for _, fn := range grgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*grgb.flds)+len(grgb.fns))
		for _, f := range *grgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*grgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GroupRoleSelect is the builder for selecting fields of GroupRole entities.
type GroupRoleSelect struct {
	*GroupRoleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (grs *GroupRoleSelect) Aggregate(fns ...AggregateFunc) *GroupRoleSelect {
	grs.fns = append(grs.fns, fns...)
	return grs
}

// Scan applies the selector query and scans the result into the given value.
func (grs *GroupRoleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, grs.ctx, ent.OpQuerySelect)
	if err := grs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GroupRoleQuery, *GroupRoleSelect](ctx, grs.GroupRoleQuery, grs, grs.inters, v)
}

func (grs *GroupRoleSelect) sqlScan(ctx context.Context, root *GroupRoleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(grs.fns))
	for _, fn := range grs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*grs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
