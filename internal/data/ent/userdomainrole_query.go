// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"stocms/internal/data/ent/predicate"
	"stocms/internal/data/ent/userdomainrole"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserDomainRoleQuery is the builder for querying UserDomainRole entities.
type UserDomainRoleQuery struct {
	config
	ctx        *QueryContext
	order      []userdomainrole.OrderOption
	inters     []Interceptor
	predicates []predicate.UserDomainRole
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserDomainRoleQuery builder.
func (udrq *UserDomainRoleQuery) Where(ps ...predicate.UserDomainRole) *UserDomainRoleQuery {
	udrq.predicates = append(udrq.predicates, ps...)
	return udrq
}

// Limit the number of records to be returned by this query.
func (udrq *UserDomainRoleQuery) Limit(limit int) *UserDomainRoleQuery {
	udrq.ctx.Limit = &limit
	return udrq
}

// Offset to start from.
func (udrq *UserDomainRoleQuery) Offset(offset int) *UserDomainRoleQuery {
	udrq.ctx.Offset = &offset
	return udrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (udrq *UserDomainRoleQuery) Unique(unique bool) *UserDomainRoleQuery {
	udrq.ctx.Unique = &unique
	return udrq
}

// Order specifies how the records should be ordered.
func (udrq *UserDomainRoleQuery) Order(o ...userdomainrole.OrderOption) *UserDomainRoleQuery {
	udrq.order = append(udrq.order, o...)
	return udrq
}

// First returns the first UserDomainRole entity from the query.
// Returns a *NotFoundError when no UserDomainRole was found.
func (udrq *UserDomainRoleQuery) First(ctx context.Context) (*UserDomainRole, error) {
	nodes, err := udrq.Limit(1).All(setContextOp(ctx, udrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userdomainrole.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) FirstX(ctx context.Context) *UserDomainRole {
	node, err := udrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserDomainRole ID from the query.
// Returns a *NotFoundError when no UserDomainRole ID was found.
func (udrq *UserDomainRoleQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = udrq.Limit(1).IDs(setContextOp(ctx, udrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userdomainrole.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) FirstIDX(ctx context.Context) string {
	id, err := udrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserDomainRole entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserDomainRole entity is found.
// Returns a *NotFoundError when no UserDomainRole entities are found.
func (udrq *UserDomainRoleQuery) Only(ctx context.Context) (*UserDomainRole, error) {
	nodes, err := udrq.Limit(2).All(setContextOp(ctx, udrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userdomainrole.Label}
	default:
		return nil, &NotSingularError{userdomainrole.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) OnlyX(ctx context.Context) *UserDomainRole {
	node, err := udrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserDomainRole ID in the query.
// Returns a *NotSingularError when more than one UserDomainRole ID is found.
// Returns a *NotFoundError when no entities are found.
func (udrq *UserDomainRoleQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = udrq.Limit(2).IDs(setContextOp(ctx, udrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userdomainrole.Label}
	default:
		err = &NotSingularError{userdomainrole.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) OnlyIDX(ctx context.Context) string {
	id, err := udrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserDomainRoles.
func (udrq *UserDomainRoleQuery) All(ctx context.Context) ([]*UserDomainRole, error) {
	ctx = setContextOp(ctx, udrq.ctx, "All")
	if err := udrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserDomainRole, *UserDomainRoleQuery]()
	return withInterceptors[[]*UserDomainRole](ctx, udrq, qr, udrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) AllX(ctx context.Context) []*UserDomainRole {
	nodes, err := udrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserDomainRole IDs.
func (udrq *UserDomainRoleQuery) IDs(ctx context.Context) (ids []string, err error) {
	if udrq.ctx.Unique == nil && udrq.path != nil {
		udrq.Unique(true)
	}
	ctx = setContextOp(ctx, udrq.ctx, "IDs")
	if err = udrq.Select(userdomainrole.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) IDsX(ctx context.Context) []string {
	ids, err := udrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (udrq *UserDomainRoleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, udrq.ctx, "Count")
	if err := udrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, udrq, querierCount[*UserDomainRoleQuery](), udrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) CountX(ctx context.Context) int {
	count, err := udrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (udrq *UserDomainRoleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, udrq.ctx, "Exist")
	switch _, err := udrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (udrq *UserDomainRoleQuery) ExistX(ctx context.Context) bool {
	exist, err := udrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserDomainRoleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (udrq *UserDomainRoleQuery) Clone() *UserDomainRoleQuery {
	if udrq == nil {
		return nil
	}
	return &UserDomainRoleQuery{
		config:     udrq.config,
		ctx:        udrq.ctx.Clone(),
		order:      append([]userdomainrole.OrderOption{}, udrq.order...),
		inters:     append([]Interceptor{}, udrq.inters...),
		predicates: append([]predicate.UserDomainRole{}, udrq.predicates...),
		// clone intermediate query.
		sql:  udrq.sql.Clone(),
		path: udrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DomainID string `json:"domain_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserDomainRole.Query().
//		GroupBy(userdomainrole.FieldDomainID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (udrq *UserDomainRoleQuery) GroupBy(field string, fields ...string) *UserDomainRoleGroupBy {
	udrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserDomainRoleGroupBy{build: udrq}
	grbuild.flds = &udrq.ctx.Fields
	grbuild.label = userdomainrole.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DomainID string `json:"domain_id,omitempty"`
//	}
//
//	client.UserDomainRole.Query().
//		Select(userdomainrole.FieldDomainID).
//		Scan(ctx, &v)
func (udrq *UserDomainRoleQuery) Select(fields ...string) *UserDomainRoleSelect {
	udrq.ctx.Fields = append(udrq.ctx.Fields, fields...)
	sbuild := &UserDomainRoleSelect{UserDomainRoleQuery: udrq}
	sbuild.label = userdomainrole.Label
	sbuild.flds, sbuild.scan = &udrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserDomainRoleSelect configured with the given aggregations.
func (udrq *UserDomainRoleQuery) Aggregate(fns ...AggregateFunc) *UserDomainRoleSelect {
	return udrq.Select().Aggregate(fns...)
}

func (udrq *UserDomainRoleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range udrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, udrq); err != nil {
				return err
			}
		}
	}
	for _, f := range udrq.ctx.Fields {
		if !userdomainrole.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if udrq.path != nil {
		prev, err := udrq.path(ctx)
		if err != nil {
			return err
		}
		udrq.sql = prev
	}
	return nil
}

func (udrq *UserDomainRoleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserDomainRole, error) {
	var (
		nodes = []*UserDomainRole{}
		_spec = udrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserDomainRole).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserDomainRole{config: udrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, udrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (udrq *UserDomainRoleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := udrq.querySpec()
	_spec.Node.Columns = udrq.ctx.Fields
	if len(udrq.ctx.Fields) > 0 {
		_spec.Unique = udrq.ctx.Unique != nil && *udrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, udrq.driver, _spec)
}

func (udrq *UserDomainRoleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userdomainrole.Table, userdomainrole.Columns, sqlgraph.NewFieldSpec(userdomainrole.FieldID, field.TypeString))
	_spec.From = udrq.sql
	if unique := udrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if udrq.path != nil {
		_spec.Unique = true
	}
	if fields := udrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userdomainrole.FieldID)
		for i := range fields {
			if fields[i] != userdomainrole.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := udrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := udrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := udrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := udrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (udrq *UserDomainRoleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(udrq.driver.Dialect())
	t1 := builder.Table(userdomainrole.Table)
	columns := udrq.ctx.Fields
	if len(columns) == 0 {
		columns = userdomainrole.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if udrq.sql != nil {
		selector = udrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if udrq.ctx.Unique != nil && *udrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range udrq.predicates {
		p(selector)
	}
	for _, p := range udrq.order {
		p(selector)
	}
	if offset := udrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := udrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserDomainRoleGroupBy is the group-by builder for UserDomainRole entities.
type UserDomainRoleGroupBy struct {
	selector
	build *UserDomainRoleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (udrgb *UserDomainRoleGroupBy) Aggregate(fns ...AggregateFunc) *UserDomainRoleGroupBy {
	udrgb.fns = append(udrgb.fns, fns...)
	return udrgb
}

// Scan applies the selector query and scans the result into the given value.
func (udrgb *UserDomainRoleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udrgb.build.ctx, "GroupBy")
	if err := udrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserDomainRoleQuery, *UserDomainRoleGroupBy](ctx, udrgb.build, udrgb, udrgb.build.inters, v)
}

func (udrgb *UserDomainRoleGroupBy) sqlScan(ctx context.Context, root *UserDomainRoleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(udrgb.fns))
	for _, fn := range udrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*udrgb.flds)+len(udrgb.fns))
		for _, f := range *udrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*udrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserDomainRoleSelect is the builder for selecting fields of UserDomainRole entities.
type UserDomainRoleSelect struct {
	*UserDomainRoleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (udrs *UserDomainRoleSelect) Aggregate(fns ...AggregateFunc) *UserDomainRoleSelect {
	udrs.fns = append(udrs.fns, fns...)
	return udrs
}

// Scan applies the selector query and scans the result into the given value.
func (udrs *UserDomainRoleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udrs.ctx, "Select")
	if err := udrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserDomainRoleQuery, *UserDomainRoleSelect](ctx, udrs.UserDomainRoleQuery, udrs, udrs.inters, v)
}

func (udrs *UserDomainRoleSelect) sqlScan(ctx context.Context, root *UserDomainRoleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(udrs.fns))
	for _, fn := range udrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*udrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
